-add support for more than 50 playlists
-add song position support
-use the built in next instead of offset
-update help to reflect changes


-probably change to cache the entire playlist all at once, to avoid weird offset related things
